# سجل التعديلات — Fares

- جعل استعادة الجلسات تعتمد على بيانات الاعتماد المحفوظة، مع عدم حذف الرقم من قاعدة البيانات عند تعذر القراءة المؤقتة لملفات الجلسة. الحذف يتم فقط بعد تأكيد تسجيل الخروج من واتساب.
- إضافة ترقية إعدادات الجلسات القديمة مرة واحدة إلى الإعدادات الافتراضية الجديدة، مع إبقاء الإيموجي السابق لكل رقم.
- منع تكرار إشعار الرسائل المحذوفة باستخدام مفتاح المحادثة والرسالة، وإيقاف تشغيله على الحالات والمجموعات.
- جعل خيار الرسائل المحذوفة خاصاً بالمحادثات الخاصة فقط، ويعمل عند تفعيل `antiDeleteMessages`.
- تحسين حفظ الحالات المحذوفة مع رسالة واضحة: «تم حذف حالة من قبل هذا الشخص.» وإظهار الرقم الكامل عند توفر رقم PN أو بديل صالح.
- منع استدعاء معالج الرسائل ومعالج الحالات معاً في حدث `messages.delete`.
- إضافة الرابطين `/bot/setting` و`/bot/settings` لفتح صفحة إعدادات المشروع نفسها.

## ملاحظة المقارنة

الرابط المرجعي `https://new11.redqueen.online/bot/setting` أعاد صفحة الهبوط العامة ولم يعرض نموذج إعدادات أو قائمة حقول قابلة للقراءة؛ لذلك لم تتم إضافة أسماء إعدادات غير مؤكدة. بقيت جميع إعدادات المشروع الحالية كما هي، وتستمر لوحة الرقم في تطبيقها مباشرة عبر واجهة API الحالية.
